# Legacy Source Files

These files are kept for reference only.

The active deployable app is:

```text
hosted/index.html
```

Original root files such as `je_scoring_calculator.html`, `app.js.txt`, `index.html.txt`, and `style.css.txt` were source fragments from the initial calculator and should not be deployed directly.

